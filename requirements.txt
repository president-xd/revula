# Core MCP + analysis (always installed)
mcp>=1.0.0
jsonschema>=4.20.0
pydantic>=2.7.0
capstone>=5.0.0
lief>=0.14.0
pefile>=2023.2.7
pyelftools>=0.30
yara-python>=4.3.0

# Optional — dynamic instrumentation
frida-tools>=12.0.0
frida>=16.0.0

# Optional — radare2 bindings
r2pipe>=1.8.0

# Optional — network analysis
scapy>=2.5.0

# Optional — fuzzy hashing / crypto
python-tlsh>=4.5.0
ppdeep>=1.1          # pure-Python ssdeep alternative (ssdeep C-ext breaks on Python 3.13)

# Optional — Android
androguard>=3.4.0a1
semgrep
quark-engine

# Optional — Python bytecode decompilation
uncompyle6>=3.9.0

# Optional — FLARE tools (also provide CLI commands: floss, capa)
flare-floss>=3.0.0
flare-capa>=7.0.0

# Optional — emulation backend for shellcode test_emulate
unicorn>=2.0.0

# Optional — exploit helper CLIs
ROPGadget
ropper
pwntools>=4.13.0

# NOTE: angr is intentionally omitted from this flat requirements file because it's ~2 GB.
#   Use extras when desired: pip install "revula[angr]" (or revula[full]).
#
# NOTE: Triton (binary-analysis DSE) has no maintained Linux PyPI wheel.
#   Upstream source build is required: https://github.com/JonathanSalwan/Triton
